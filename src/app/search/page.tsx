'use client'; import { useState } from 'react'; import { Search } from 'lucide-react'; import { products } from '@/lib/content'; import { ProductCard } from '@/components/store/ProductCard';
export default function SearchPage() { const [query,setQuery] = useState(''); const results = products.filter((p) => `${p.name} ${p.gemstone} ${p.category} ${p.purpose.join(' ')} ${p.zodiac.join(' ')}`.toLowerCase().includes(query.toLowerCase())); return <div className="page-wrap search-page"><p className="eyebrow">SEARCH THE COLLECTION</p><h1>What are you looking for?</h1><div className="search-input"><Search/><input autoFocus value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search stones, jewellery, collections…"/></div>{query ? <><p className="count">{results.length} results for “{query}”</p><div className="product-grid">{results.map((p) => <ProductCard product={p} key={p.id}/>)}</div></> : <div className="search-suggestions"><p>Popular searches</p>{['Ruby','Emerald','Blue Sapphire','Bracelets','Protection'].map((term) => <button onClick={() => setQuery(term)} key={term}>{term}</button>)}</div>}</div>; }
