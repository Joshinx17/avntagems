import Link from 'next/link'; import { ArrowRight } from 'lucide-react'; import { articles } from '@/lib/content'; export const metadata={title:'Journal'}; export default function Journal(){return <div className="page-wrap journal"><p className="eyebrow">THE AVNTA JOURNAL</p><h1>More than a beautiful stone.</h1><p className="intro">Guides, care notes and introductions for a more considered jewellery collection.</p><div className="article-grid">{articles.map((article,index)=><Link href={`/journal/${article.slug}`} key={article.slug}><span>0{index+1}</span><p className="eyebrow">{article.category}</p><h2>{article.title}</h2><p>{article.excerpt}</p><i>Read article <ArrowRight size={16}/></i></Link>)}</div></div>}
