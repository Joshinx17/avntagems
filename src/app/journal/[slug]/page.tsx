import Link from 'next/link'; import { notFound } from 'next/navigation'; import { articles } from '@/lib/content'; export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const article=articles.find((item)=>item.slug===slug);if(!article)notFound();return <article className="article page-wrap"><p className="eyebrow">{article.category}</p><h1>{article.title}</h1><p className="article-lede">{article.excerpt}</p><div className="article-body"><p>This demonstration article is a content-model placeholder. Before publishing, replace this text with editorial content reviewed for its accuracy and sourced appropriately.</p><h2>A considered approach</h2><p>Gemstones are individual objects. Details including treatment, origin, certification and care need to be product specific and verified by AVNTA GEMS before making a customer-facing claim.</p><p>Traditional gemstone associations are part of cultural and personal practices; they are not medical or scientific claims.</p></div><Link href="/journal" className="text-link">← Back to journal</Link></article>}
