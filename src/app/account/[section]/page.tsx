import Link from 'next/link'; export default async function AccountSection({params}:{params:Promise<{section:string}>}){const {section}=await params;return <div className="page-wrap account"><p className="eyebrow">ACCOUNT / {section.toUpperCase()}</p><h1>{section.replaceAll('-',' ')}</h1><p>This account area is structured as a safe integration point for your authentication, customer and order system.</p><Link href="/account" className="button button-dark">Back to account</Link></div>}
