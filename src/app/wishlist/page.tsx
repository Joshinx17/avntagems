'use client'; import Link from 'next/link'; import { Heart } from 'lucide-react'; import { products } from '@/lib/content'; import { useCommerce } from '@/components/store/CommerceProvider'; import { ProductCard } from '@/components/store/ProductCard';
export default function WishlistPage() { const { wishlist } = useCommerce(); const saved = products.filter((p) => wishlist.includes(p.id)); return <div className="page-wrap wishlist-page"><p className="eyebrow">SAVED FOR LATER</p><h1>Your wishlist</h1>{saved.length ? <div className="product-grid">{saved.map((p) => <ProductCard key={p.id} product={p}/>)}</div> : <div className="empty-state"><Heart size={30}/><h3>Your wishlist is still open.</h3><p>Save pieces to return to them later.</p><Link href="/shop" className="button button-dark">Explore the collection</Link></div>}</div>; }
